#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../statsd/bin/statsd" "$@"
  ret=$?
else 
  node  "$basedir/../statsd/bin/statsd" "$@"
  ret=$?
fi
exit $ret
